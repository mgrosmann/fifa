sql15 -e "SELECT players.playerid AS \`info.player.id\`,
          pn1.name AS \`info.name.firstname\`,
          pn2.name AS \`info.name.lastname\`,
          pn3.name AS \`info.name.knownas\`,
          pn4.name AS \`info.name.playerjerseyname\`
FROM players
INNER JOIN playernames pn1 ON players.firstnameid = pn1.nameid
INNER JOIN playernames pn2 ON players.lastnameid = pn2.nameid
INNER JOIN playernames pn3 ON players.commonnameid = pn3.nameid
INNER JOIN playernames pn4 ON players.playerjerseynameid = pn4.nameid;;" > pn.txt
tr '\t' ';' < pn.txt > pn.csv
tr '\t' ';' < players.txt > players.csv

new.py genere 2 csv -> nouveau joueur et joueur existant
name.py -> indique les slots nécéssaires dans playernames
name1.py analyse nouveau joueur et les envoie dans joueur existant si correspondance
(corriger gonzalo garcia et raul asensio, les renvoyé des nouveaux, erreur de name1.py)
name2.py se base sur le playerid indique les différences de nom pour 1 meme playerid (permet d'identifier quand 1 Playerid est occupé
par un joueur qui ne devrait pas)

366